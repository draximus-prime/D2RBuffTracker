<ui:FluentWindow x:Class="D2RBuffTracker.Views.TextInputWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Input"
                 Width="420" Height="220"
                 Background="{DynamicResource ApplicationBackgroundBrush}"
                 WindowBackdropType="None"
                 ExtendsContentIntoTitleBar="True"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="Bar" Title="Input" ShowMaximize="False" ShowMinimize="False" />

        <StackPanel Grid.Row="1" Margin="20,12,20,0">
            <TextBlock x:Name="LabelText" Margin="0,0,0,8" />
            <ui:TextBox x:Name="Input" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,16,20,20">
            <ui:Button Content="Cancel" Click="Cancel_OnClick" MinWidth="90" />
            <ui:Button Content="OK" Appearance="Primary" Margin="8,0,0,0" MinWidth="90"
                       Click="Ok_OnClick" IsDefault="True" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
